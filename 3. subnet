Create Subnet
FIRST AVAILABILITY ZONE
select your VPC
give it a name: public subnet az1
Availability Zone : us-east-1a
Ipv4 subnet CIDR Block : 10.0.0.0/24
Auto-Assign IP ---- action ---- edit subnet setting----check enable auto-assign ip

select your VPC
give it a name: private app subnet az1
Availability Zone : us-east-1a
Ipv4 subnet CIDR Block : 10.0.2.0/24

select your VPC
give it a name: private data subnet az1
Availability Zone : us-east-1a
Ipv4 subnet CIDR Block : 10.0.4.0/24

SECOND AVAILABILITY ZONE
select your VPC
give it a name: public subnet az2
Availability Zone : us-east-1a
Ipv4 subnet CIDR Block : 10.0.1.0/24
Auto-Assign IP ---- action ---- edit subnet setting----check enable auto-assign ip

select your VPC
give it a name: private app subnet az2
Availability Zone : us-east-1a
Ipv4 subnet CIDR Block : 10.0.3.0/24

select your VPC
give it a name: private data subnet az1
Availability Zone : us-east-1a
Ipv4 subnet CIDR Block : 10.0.5.0/24